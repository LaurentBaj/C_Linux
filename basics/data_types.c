#include <stdio.h>

int main() {

    int a = 3, b = 2; 
    char h = 'H', e = 'e', l = 'l', o = 'o'; 

    printf("\n%d + %d = %d\n", a, b, a + b); // 3 + 2 = 5
    printf("%c%c%c%c%c\n\n", h, e, l, l, o); // Hello

    return 0; // Main does not need a return type since 0 is by default 
}